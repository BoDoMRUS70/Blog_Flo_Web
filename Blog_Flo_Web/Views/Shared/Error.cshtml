@model Blog_Flo_Web.Business_model.Models.ErrorViewModel
@{
    ViewData["Title"] = "Ошибка";
}

<div class="text-center">
    <h1 class="text-danger">Что-то пошло не так</h1>
    <h2 class="text-danger">Произошла ошибка при обработке вашего запроса.</h2>

    @if (Model.ShowRequestId)
    {
        <p>
            <strong>Request ID:</strong> <code>@Model.RequestId</code>
        </p>
    }

    @if (Model.StatusCode.HasValue)
    {
        <p>
            <strong>Код ошибки:</strong> <code>@Model.StatusCode</code>
        </p>
    }

    <p>
        Пожалуйста, попробуйте позже или свяжитесь с поддержкой.
    </p>

    @if (Model.IsDevelopment)
    {
        <h3>Режим разработки</h3>
        <p>
            Включение <strong>Development</strong> среды покажет более детальную информацию об ошибке.
        </p>
        <p>
            <strong>Режим разработки не должен быть включен в продакшене.</strong>
            Это может привести к отображению чувствительной информации для конечных пользователей.
            Для локальной отладки включите <strong>Development</strong> среду, установив переменную окружения <strong>ASPNETCORE_ENVIRONMENT</strong> в значение <strong>Development</strong>
            и перезапустите приложение.
        </p>
    }
</div>